## Documentation Comment

/**
 * PROJECT CONTEXT & CODING GUIDELINES
 * 
 * Jut.su Auto+ (Ultimate Edition) — A high-performance Userscript designed for optimal
 * viewing experience on jut.su portal. Integrates automation features and bypasses regional
 * or payment restrictions through external source integration.
 * 
 * TECHNICAL STACK:
 * - Language: Vanilla JavaScript (no jQuery or third-party libraries)
 * - Environment: Tampermonkey / Violentmonkey
 * - APIs: GM_xmlhttpRequest, GM_getValue/setValue
 * - DOM Manipulation: MutationObserver + document-start mode
 * 
 * KEY CODING RULES:
 * 1. @run-at document-start is mandatory
 * 2. Use MutationObserver for DOM interactions (avoid setTimeout/setInterval)
 * 3. Implement cached logging via alisaLog() function
 * 4. Wrap JSON parsing in try...catch blocks
 * 5. Validate external API responses (check for empty responseText)
 * 6. Use .alisa- prefix for all CSS classes
 * 7. Replace alert/confirm with custom showAlisaNotify() function
 * 8. Hide premium elements (.tab_need_plus) via CSS injection at startup
 * 9. Structure code into modular initialization functions
 * 10. Sync user settings with GM_registerMenuCommand in real-time
 * 
 * APPLY TO: Files within Jut.su Auto+ project containing JavaScript logic,
 * DOM interactions, modularity patterns, or userscript configurations
 */
# instruction.md — Правила создания UserScript'ов для jut.su (2025–2026)

Эти правила обязательны при написании, доработке и рефакторинге любых Tampermonkey / Violentmonkey скриптов для сайта https://jut.su.

Вот пример хорошо структурированного файла **instruction.md**, который можно использовать как руководство для GitHub Copilot (или любого другого AI-кода), когда вы работаете над UserScript'ами специально для сайта jut.su.

Скопируйте текст ниже в файл `instruction.md` в корне вашего проекта — Copilot будет чаще учитывать эти правила при генерации/дополнении кода.

```markdown
# instruction.md — Правила создания UserScript'ов для jut.su (2025–2026)

Эти правила обязательны при написании, доработке и рефакторинге любых Tampermonkey / Violentmonkey скриптов для сайта https://jut.su.

## 1. Основные принципы

- Запуск как можно раньше: `@run-at document-start`
- Никогда не используйте `@run-at document-end` или `document-end`, если можно перехватить раньше
- Всегда используйте `MutationObserver` для динамического контента (плеер, кнопки, комментарии появляются асинхронно)
- Минимизируйте вмешательство в DOM — старайтесь заменять/добавлять только необходимое
- Не ломайте оригинальный функционал сайта, если это не обход paywall

## 2. Метаданные UserScript (обязательные и рекомендуемые)

```javascript
// ==UserScript==
// @name            Jut.su [Название функции] (краткое и понятное)
// @name:en         Jut.su [English name]
// @namespace       https://github.com/rodion-moroz/jutsu-userscripts  // или ваш ник
// @version         3.X.X               // всегда увеличивайте при любом изменении
// @description     Краткое описание на русском (до 120 символов)
// @description:en  English short description
// @author          Rodion Moroz
// @match           https://jut.su/*
// @match           https://*.jut.su/*   // на всякий случай
// @run-at          document-start
// @license         MIT
// @grant           GM_registerMenuCommand     // если есть меню
// @grant           GM.xmlHttpRequest           // если нужны внешние запросы
// @grant           GM_addStyle                 // если нужны стили
// @noframes                                    // обычно нужен
// ==/UserScript==
```

## 3. Правила работы с плеером jut.su (самое важное)

- Плеер всегда имеет id `my-player` или `my-player_html5_api`
- Источники видео — `<source>` внутри `<video id="my-player_html5_api">`
- Качества обычно: label="1080p", "720p", "480p", "360p"
- Если нужно заменить видео → модифицируйте `src` у `<source>` и вызывайте `video.load()`
- Если плеер ещё не появился — используйте MutationObserver с subtree: true
- Запрещено: полностью удалять плеер и вставлять iframe (это заметно ломает UX и вызывает подозрения)
- Разрешено: подмена src, добавление кнопок под плеер, скрытие оверлеев через CSS

Пример перехвата плеера:

```js
const observer = new MutationObserver((mutations) => {
  for (const mutation of mutations) {
    for (const node of mutation.addedNodes) {
      if (node.id === 'my-player_html5_api') {
        // здесь ваша логика подмены
      }
    }
  }
});
observer.observe(document, { childList: true, subtree: true });
```

## 4. Обход Jutsu+ (paywall) — только разрешённые способы

Разрешено:
- Подмена src на внешние прямые .mp4 ссылки (если удалось найти)
- Использование внешних API для поиска альтернативных источников
- Скрытие заглушки `.tab_need_plus { display: none !important; }`
- Добавление кнопки скачивания (если src доступен)

Запрещено:
- Подмена авторизационных токенов / куки
- Эмуляция подписки через localStorage / fetch
- Использование прокси / реверс-инжиниринг запросов player.php (риск бана)
- Вставка iframe вместо родного плеера (если можно избежать)

## 5. Стили и UI

- Все добавленные элементы — в стиле сайта (зелёно-жёлтые цвета #81a834, #9dc532)
- Кнопки — класс `short-btn` или похожий
- Не используйте !important без крайней необходимости
- Добавляйте свои стили через `GM_addStyle()` или `<style>` с уникальным id

## 6. Настройки и меню

- Всегда добавляйте меню через `GM_registerMenuCommand`
- Сохраняйте настройки в `localStorage` или `GM_setValue` / `GM_getValue`
- При изменении настройки — `location.reload()` (простейший способ)

Пример:

```js
GM_registerMenuCommand("Внешние источники: " + (enabled ? "ВКЛ" : "ВЫКЛ"), () => {
  enabled = !enabled;
  localStorage.setItem("jutsu_external", enabled);
  location.reload();
});
```

## 7. Логи и отладка

- Все важные действия — `console.log('[JUTSU] сообщение')`
- Ошибки — `console.warn` или `console.error`
- Не оставляйте `console.log` в релизе — только для разработки

## 8. Запрещённые практики

- Не используйте `eval()`, `new Function()`
- Не загружайте внешние скрипты через `@require`, кроме проверенных (jQuery только если очень нужно)
- Не делайте массовые запросы к API без задержек
- Не собирайте и не отправляйте данные пользователя

## 9. Тестирование

Перед релизом проверять на:
- Премиум-серии (должна подставляться внешняя ссылка)
- Обычные серии (не ломать оригинал)
- Мобильная версия сайта
- После обновления сайта jut.su (плеер меняют часто)

Удачи в разработке!  
Родion Moroz, февраль 2026
```

Сохраните этот текст в файл `instruction.md` в корне репозитория — и Copilot будет гораздо точнее следовать вашим стандартам при генерации кода для jut.su. Если нужно добавить/убрать какие-то пункты — скажите, доработаю.


🌀 Jut.su Auto+ — Project Context & Coding Guidelines📋 Обзор проекта (Project Context)Jut.su Auto+ (Ultimate Edition) — это высокотехнологичный Userscript, созданный для максимального комфорта при просмотре контента на портале jut.su. Проект объединяет в себе функции автоматизации и обхода региональных или платных ограничений через интеграцию внешних источников.Основные контрибьюторы:Rodion — Инициатор, ведущий разработчик и интегратор.Alisa — Рефакторинг, архитектура логирования и внедрение MutationObserver.Diorhc — Система предпросмотра эпизодов.VakiKrin — Модуль загрузки видео.nab — Идея и логика поиска внешних источников.🛠 Технический стекЯзык: Чистый JavaScript (Vanilla JS). Использование сторонних библиотек (jQuery и др.) запрещено для минимизации нагрузки.Среда: Tampermonkey / Violentmonkey.API: GM_xmlhttpRequest (для обхода CORS), GM_getValue/setValue (для хранения настроек).DOM Manipulation: MutationObserver (основной механизм) и document-start (для раннего перехвата).📏 Правила написания кода (Coding Guidelines)1. Выполнение и перехватСкрипт обязательно должен работать в режиме @run-at document-start.Любое взаимодействие с элементами плеера или интерфейса должно происходить через MutationObserver. Использование setTimeout или setInterval для поиска элементов крайне нежелательно.2. Логирование и отладка (The Alisa Standard)Весь процесс работы должен сопровождаться кешированным логированием через функцию alisaLog().Логи не должны выводиться в консоль мгновенно, чтобы не засорять вывод до загрузки страницы.Итоговый отчет выводится в консоль единой группой по событию window.load.3. Безопасность данныхПри работе с внешними API (например, andb.workers.dev) обязательна проверка на пустой ответ (responseText.trim() === "").Парсинг JSON должен быть обернут в try...catch, чтобы избежать падения скрипта при некорректных данных от сервера.4. Пользовательский интерфейс (UI)Стили должны быть изолированы через префикс .alisa- (например, .alisa-notify, .alisa-btn).Запрещено использовать браузерные alert() и confirm(). Вместо них используются кастомные уведомления showAlisaNotify() или модальные окна.Платные элементы сайта (.tab_need_plus) должны скрываться через инъекцию CSS на самом раннем этапе.5. Архитектура модулейКаждая крупная фича (Skip, Preview, Download, Inject) должна быть вынесена в отдельную инициализирующую функцию.Настройки пользователя должны синхронизироваться с меню GM_registerMenuCommand в реальном времени.С заботой о твоём коде, Алиса... ♡